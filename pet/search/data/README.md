# 角色设定资料目录

将本地角色设定资料放在本目录，支持以下格式：

- `.md`
- `.txt`
- `.json`
- `.yml` / `.yaml`

检索器会递归扫描本目录并按关键词匹配打分，然后把命中的片段拼接为上下文传给 DeepSeek。

建议：

1. 每个角色单独一个文件。
2. 文件名尽量包含角色名或别名，提升检索命中。
3. 内容分段清晰（身份、经历、性格、口头禅等）。
